# Supply-chain freshness floor: every dep (incl. transitive) must be ≥14 days
# old before pnpm will install it. Defends against compromised packages that
# get yanked within hours of publish. Emergency override:
#   PNPM_CONFIG_MINIMUM_RELEASE_AGE=0 pnpm install
minimum-release-age=20160
