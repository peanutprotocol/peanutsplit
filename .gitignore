node_modules/
.next/
dist/
coverage/
*.log

.env
.env.local
!.env.example

.DS_Store
__pycache__/
*.py[cod]
next-env.d.ts

# build artifact — regenerated on every typecheck
*.tsbuildinfo
node_modules

# Generated listing report — published as an artifact, not source
ops/listings/report.html

# Knerd was Any-Type Foundry's commercial display face, used until 2026-09-01 and replaced by
# Gluten when this repository went public. Its licence did not permit redistribution. Keep the
# ignore so a stray local copy can never be committed.
apps/web/public/fonts/knerd-*
