node_modules/
.next/
dist/
coverage/
*.log

.env
.env.local
!.env.example

.DS_Store
__pycache__/
*.py[cod]
next-env.d.ts

# build artifact — regenerated on every typecheck
*.tsbuildinfo
node_modules

# Generated listing report — published as an artifact, not source
ops/listings/report.html

# Knerd is Any-Type Foundry's and licensed to Squirrel Labs Ltd for use, not for redistribution, so
# the files must never be committed to this public repository. Drop a local copy in to build the
# official look; see LICENSES/LicenseRef-Knerd-Commercial.txt.
apps/web/public/fonts/knerd-*
apps/web/src/assets/fonts/

# The box-resident supervisor's incident log. It was purged from this repository's history before
# publication: it carries Hetzner paths, bot token scopes and prod monitoring gaps, none of which
# belong in a public repo. It lives on the box. Do not commit it back.
ops/steward/
